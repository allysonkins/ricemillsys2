<?php
declare(strict_types=1);

namespace App\Controller\Component;

use Cake\Controller\Component;

/**
 * Minimal RequestHandlerComponent placeholder.
 *
 * Some parts of the app (or plugins) may expect this component to exist.
 * Create a minimal implementation so the app can load the component without errors.
 */
class RequestHandlerComponent extends Component
{
    // Intentionally minimal. Add helper methods here if your app requires them.
}
